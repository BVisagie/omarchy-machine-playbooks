---
id: REPLACE-HOSTNAME
title: Human name (e.g. Dev laptop)
hostname: REPLACE-HOSTNAME
match:
  motherboard: ""
  gpu_pci: ""
  monitor_edid: ""
---

# REPLACE title

Fill match keys from `hostnamectl`, `lspci -nn`, and `hyprctl monitors` so a renamed host still maps here.

## Hardware

- CPU:
- GPU:
- Display:

## Notes

Playbooks go in `playbooks/`. Apply `common/` first after a format.
